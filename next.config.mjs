/** @type {import('next').NextConfig} */
const nextConfig = {
    trailingSlash: true,
    images: {
      domains: ['mbaroi.in'],  // Add your image domain here
    },
  };
  
  export default nextConfig;
